<%= form_tag(posts_path, :method => "get", remote: true, id: "search-form", class: "navbar-form") do %>
  <div class="input-group add-on">
    <%= text_field_tag :search, params[:search], placeholder: "Search for posts..", class: "form-control" %>
    <div class="input-group-btn">
      <%= button_tag(type: "submit", class: "btn btn-default") do %>
        <i class="glyphicon glyphicon-search"></i>
      <% end %>
    </div>
  </div>
<% end %>
<h1>Новостная лента</h1>
<!-- TODO: add Bootstrap alerts -->
<div id="alert_placeholder"></div>
<% if current_user.present? && (can? :create, Post) %>
    <p><%= link_to "Новая статья", new_post_path, remote: true, class: "btn btn-primary" %></p>
<% end %>
  <div id="posts-list">
    <%= render partial: "posts" %>
  </div>
<div class="new-post"></div>
<div id="post-modal" class="modal fade"></div>
<div id="FoundPosts" class="modal fade"></div>
